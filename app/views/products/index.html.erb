<p id="notice"><%= notice %></p>

<h1>Jeans for All of Us</h1>

<table class="table">
  <thead>
    <tr>
      <th>Name</th>
      <th>Price</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @products.each do |product| %>
      <tr>
        <td><%= product.name %></td>
        <td><%= number_to_currency(product.price) %></td>
        <td><%= link_to 'Show', product %></td>
        <td><%= "photo will go here" %></td>
        <!-- <td><%= link_to 'Edit', edit_product_path(product) %></td> --> <!-- get rid of this --> 
        <!-- <td><%= link_to 'Destroy', product, method: :delete, data: { confirm: 'Are you sure?' } %></td> --> <!-- and this --> 
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<!-- <%= link_to 'New Product', new_product_path %> --> <!-- and this --> <!-- also fix the routes -->
